apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-rating
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/miserybee99/devops-project.git
      targetRevision: feature/gitops
      path: k8s/charts/rating
      helm:
        valueFiles:
          - $values/overlays/dev/rating.yaml
    - repoURL: https://github.com/Phuc-215/yas-deploy.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
